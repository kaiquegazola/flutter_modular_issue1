name: flutter_modular_issue
description: A new Flutter application.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flutter_modular: ^3.0.0-nullsafety.45

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true